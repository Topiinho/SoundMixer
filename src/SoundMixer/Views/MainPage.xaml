<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SoundMixer.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="40" MaxWidth="400">
            <StackPanel Spacing="12" HorizontalAlignment="Center">
                <TextBlock Text="SoundMixer" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
                <TextBlock Text="Controle de Volume por Aplicativo" HorizontalAlignment="Center" Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}"/>
            </StackPanel>

            <StackPanel Spacing="12">
                <Button Content="🔊 Mixer" Padding="16,12" HorizontalContentAlignment="Center" FontSize="16" Command="{x:Bind ViewModel.NavigateToMixerCommand}"/>
                <Button Content="⚙️ Configurações" Padding="16,12" HorizontalContentAlignment="Center" FontSize="16" Command="{x:Bind ViewModel.NavigateToSettingsCommand}"/>
            </StackPanel>

            <TextBlock Text="v1.0" HorizontalAlignment="Center" Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}" FontSize="12"/>
        </StackPanel>
    </Grid>
</Page>
